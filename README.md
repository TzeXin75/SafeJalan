# SafeJalan

Road damage reporting app (React 19 + Vite + Tailwind 4 + Leaflet). Exported from Figma Make.

## Run locally
```bash
pnpm install   # or: npm install
pnpm dev       # or: npm run dev
```

## Build
```bash
pnpm build
```
